//后端
const server_ip = '119.3.127.254';
const local_ip = 'localhost'

export const apiUrl = `http://${server_ip}:8080`;
// export const apiUrl = `https://${local_ip}:8080`;
export const nodeServerUrl = `http://${server_ip}:5000`;
